# R26 decision ledger

1. The owner's July 19 instruction authorizes one final geometry-to-art
   reconciliation after the Act 1 painting was redone.
2. Smooth intermediate curves may follow visible roads and clearings; noisy
   pixel-edge tracing is forbidden.
3. “Coral Reef” and “Coastal Reef” are the same `coastalReef` dungeon.
4. The old native anchor `(1690,2410)` was 263.05 world pixels before the visible
   cave foot. R26 restores one bounded painted connector and moves only this
   native art-space endpoint to `(1877,2596)`.
5. The route ID `port-sapphire-to-coastal-reef`, `drakeCargo` guard, and
   southeast Port component are unchanged.
6. Port's three terminals and components, Sunken's exact 11-wide straight
   throat, Crystal's dynamic seal, the seven-route graph, and all forbidden
   shortcuts are locked.
7. The first connector chord was visually rejected because it crossed turquoise
   shallows. The single permitted fix follows the painted south-then-east dirt
   path. Fresh static and visual reviewers returned GO afterward.
8. R26 is a design authority pack, not a runtime promotion. Runtime integration
   requires a separate explicit task.
9. After this lock, ordinary Act 1 visual corrections are art-only. Topology,
   endpoint, width, blocker, or connectivity changes require a new explicit
   gameplay decision and may not restart an automatic art/polygon loop.
