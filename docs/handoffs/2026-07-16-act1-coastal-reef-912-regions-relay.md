---
date: 2026-07-16
type: handoff
project: edu-rpg
milestone: act1-coastal-reef-912-regions
status: active
supersedes: docs/handoffs/2026-07-16-act1-deep-sunken-outer-west-912-regions-relay.md
relay_chain_id: edu-rpg-act1-overhaul
relay_sequence: "08"
relay_status: delegated
relay_predecessor_thread_id: 019f6a8e-f6c5-7c01-891e-a20a1b5fb949
relay_successor_thread_id: 019f6ac2-59ab-7fe1-9397-3011e84d4ae6
subagents_drained: true
background_sessions_drained: true
---

# All seven locked routes authored; Relay 09 may audit off-route residual

## Outcome

Relay 08 audited every exact `852 x 1846` locked-phone draw and preload
footprint after the 15-region revision-7 manifest. Coastal Reef was the only
route below 95% authored coverage. Exhaustive 201-sample-per-segment analysis
proved that one 512-world region cannot close both residual vertical extremes.

The smallest useful batch is exactly:

- `coastal-channel-912-v1` at `[1696,1888,512,512]`;
- `coastal-reef-912-v1` at `[1568,2144,512,512]`.

The pair gives 100% raw authored coverage at every sampled Coastal route
position. Both regions require only Coastal-route affinity; without affinity,
Millbrook-Port reaches `5/4` and Port-Crystal reaches `5/5`. No progress window
is needed. Final exhaustive route peaks remain `4/4`, `4/3`, `4/3`, `4/4`,
`4/4`, `4/4`, `4/4`.

Composition references were captured before generation. Two built-in image
generation calls produced two accepted, genuinely new `1254 x 1254` RGB
masters. A separate deterministic exact `57/32` shared lattice, global
192-color no-dither palette, directed `y+` overlap crossfade, exact `912 x 912`
RGBA layers, and 24-world feather preserve every accepted Relay 01-07 byte.

## Verification

PASS:

- exact 14,271-sample residual audit and one-region impossibility proof;
- complete two-region raw Coastal footprint closure and required affinity;
- in-memory/disk byte equality for the shared composite and all twelve layers;
- transparent per-file inventories for all accepted Relay 01-07 artifacts and
  the complete 22-file Relay 08 batch snapshot;
- manifest revision `8`, path revision `2`, and exact `2368 x 2912` chunk
  reconstruction;
- full map-engine, path, collision, blocker, seal, semantic, retained-gate,
  save/replay, snapshot, and runtime-override suites;
- exact `852 x 1846`, 25 fps media for every route and cardinal motion; all 16
  WebMs fully decode;
- all seven endpoints and all 21 blocker probes; zero visible asset/detail
  misses;
- six-chunk/four-detail ceilings, all six Coastal resources, resident/transfer/
  decoded bytes, evictions, frames, 12.6 ms analog response, exact recenter,
  and boot-cover telemetry;
- original-resolution master, composition, lattice, route, blocker, dense
  motion, and bridge-transition review with no fuzzy boundary, rectangular
  seam, pop, flash, disappearance, duplicate bridge, input hitch, or HUD/boot
  regression;
- Python/JavaScript/TypeScript static checks, `git diff --check`, code check,
  and fresh read-only PASS at 97% confidence with no remaining P0-P3 findings.

Full verdict:
`runtime-v2/COASTAL-REEF-912-R08-VERIFICATION.md`.

Canonical evidence:
`runtime-v2/coastal-reef-912-r08/evidence/`.

Coverage/provenance:
`runtime-v2/coastal-reef-912-v1/COVERAGE-AUDIT.md` and
`runtime-v2/coastal-reef-912-v1/PROVENANCE.md`.

## Current state

- Worktree remains deliberately dirty on branch `codex/map-engine-semantic-data`
  with the inherited project history preserved.
- No commit, push, deploy, publish, release, branch/worktree creation, Vite run,
  legacy rebuild, or TestFlight/App Store Connect mutation occurred.
- Manifest is revision `8`; path constraints remain revision `2`.
- All seven locked phone-route footprints are now authored at 912.
- `fullAct1` deliberately remains false: off-route Act 1 legacy coverage is
  still explicit diagnostic fallback and has not yet been ranked by playable
  visibility or user value.

## Locked decisions

- Work only in
  `/Users/christopherhachisu/Documents/claudecode/edu-rpg-map-engine-semantic-data`.
- Preserve the shared dirty tree and every accepted Relay 01-08 artifact.
- Do not run Vite, `npm run build`, `npx vite`, or rebuild legacy shipped source.
- Never sharpen or upscale legacy pixels. Any new coverage starts from genuinely
  new high-resolution masters and a separate deterministic `912/512` lattice.
- Preserve locked composition, all seven route geometries, bridges, blockers,
  semantic transitions/saves, geometry-only collision, cardinal-only G3,
  `125 ms` `0-A-0-B`, continuous analog input, speed `52`, camera width `208`,
  HUD, Port detail, retained landmarks, and Crystal seal/mouth.
- Keep the hard six-chunk/four-detail runtime error. Use route/progress affinity
  only if exhaustive preload/draw sampling proves it necessary.
- Do not commit, push, deploy, publish, release, alter TestFlight/App Store
  Connect, create a branch/worktree, expand dungeons, or modify retained
  landmarks.

## Remaining work

Audit the exact remaining off-route legacy footprint inside the playable Act 1
surface after the 17-region manifest. Separate actually reachable/observable
phone-camera exposure from unreachable map margin and never-drawn fallback,
then rank residual coherent areas by player visibility and retained-composition
value. Author only the smallest useful bounded batch if the audit proves a
clear high-value target and all byte/stream gates can remain intact. Do not
blindly fill the whole map or claim `fullAct1` without exhaustive proof; stop at
an owner decision if no objective bounded continuation remains.

## Agent drain ledger

- `r08_coverage_audit`: completed read-only exact phone-footprint and candidate
  audit; no file ownership remained.
- `r08_pipeline_audit`: completed read-only integration and preservation audit;
  no file ownership remained.
- `r08_lattice_builder`: completed the isolated builder in
  `scripts/build_act1_coastal_reef_lattices.py`; root inspected and verified the
  file and generated outputs.
- `r08_master_batch`: completed two built-in generation calls, promoted two
  byte-identical accepted masters, and recorded provenance.
- `r08_fresh_review`: completed read-only PASS at 97% confidence after
  independent coverage, hash, reconstruction, regression, video, telemetry,
  visual, and static-code review.
- Final collaboration re-list showed only completed children; no working,
  waiting, or idle child owns unfinished work.

## Background session drain ledger

- Canonical capture unified session `11381` completed with exit `0`.
- Owned static-server session `79478` stopped with Ctrl-C and exit `0`.
- All other command cells completed in the foreground. No owned watcher,
  recorder, compiler, server, or shell remains live.

## Resume here

Read this handoff, the runtime contract, and the Relay 08 verification. Inspect
the dirty tree, revision-8 manifest, transparent byte inventories, and absence
of inherited live agents. Start with an exact playable/off-route visibility
audit; do not generate art until a bounded residual target is objectively
selected and its composition reference is captured.

## Paste-ready continuation prompt

`[relay:edu-rpg-act1-overhaul:09]` — **Act 1 Relay 09 — Off-Route Residual 912 Audit**

Work only in
`/Users/christopherhachisu/Documents/claudecode/edu-rpg-map-engine-semantic-data`.
Resume from
`/Users/christopherhachisu/Documents/claudecode/edu-rpg-map-engine-semantic-data/docs/handoffs/2026-07-16-act1-coastal-reef-912-regions-relay.md`.

Use `$relay-fresh-sessions` and `$session-relay`. Also read the edu-rpg,
coding-skill, ponytail, game-design, imagegen, and code-check skills plus parent
`/Users/christopherhachisu/Documents/claudecode/AGENTS.md`, project
`/Users/christopherhachisu/Documents/claudecode/edu-rpg/AGENTS.md`,
`docs/AGENT-WORKFLOW.md`, `docs/AUTONOMOUS-SESSION-RELAY.md`,
`design/ART-DIRECTION.md`, `design/GAME-FEEL.md`, runtime
`ACT1-HIFI-RUNTIME-CONTRACT.md`, and
`COASTAL-REEF-912-R08-VERIFICATION.md` before acting. Verify handoff metadata,
the dirty repository state, manifest revision 8, accepted-artifact inventories,
and absence of inherited live agents.

Single outcome: audit the exact remaining off-route legacy footprint inside the
playable Act 1 surface after the 17-region manifest. Separate reachable and
observable locked-phone exposure from unreachable margin and never-drawn
fallback, rank coherent residual areas by player visibility and retained-
composition value, then author only the smallest useful bounded 912 batch if a
clear objective target exists. Do not pre-assume bounds, master count, affinity,
or that more art is required. Stop at an owner decision if no bounded high-value
continuation can be proven.

If authoring is justified, capture composition references before image
generation, use genuinely new high-resolution masters, and integrate through a
separate deterministic `912/512` shared lattice that preserves every Relay
01-08 byte. Keep the hard six-chunk/four-detail ceiling and use affinity only
when exhaustive preload/draw sampling proves it necessary.

Failable proof: deterministic builders, transparent accepted-artifact byte
identity, exact chunk reconstruction, full map-engine/path/semantic regression
suites, exact `852 x 1846` affected-area and seven-route motion where runtime
changes can touch routes, explicit resource/resident/stream telemetry, no fuzzy
fallback boundary, seam, pop, flash, asset miss, input hitch, or boot regression,
plus fresh read-only review.

Preserve the locked composition, all seven routes, bridges, blockers, semantic
transitions/saves, geometry-only collision, cardinal-only G3, `125 ms`
`0-A-0-B`, continuous analog joystick, speed `52`, camera width `208`, HUD, Port
detail, retained landmarks, and Crystal seal/mouth. Never sharpen or upscale
legacy pixels. Do not run Vite, `npm run build`, `npx vite`, rebuild legacy
shipped source, commit, push, deploy, publish, release, alter TestFlight/App
Store Connect, create a branch/worktree, expand dungeons, or modify retained
landmarks.

At the next verified boundary, drain every subagent and owned background
session, write the outgoing handoff, and use `$relay-fresh-sessions` to create
exactly one Relay 10 task only if all gates pass.
