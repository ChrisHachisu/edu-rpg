---
date: 2026-07-16
type: handoff
project: edu-rpg
milestone: act1-western-hub-912-regions
status: active
supersedes: docs/handoffs/2026-07-16-act1-central-east-912-boundary-repaired-relay.md
relay_chain_id: edu-rpg-act1-overhaul
relay_sequence: "06"
relay_status: delegated
relay_predecessor_thread_id: 019f6a0b-f34a-7563-9e66-6257a2fc1d72
relay_successor_thread_id: 019f6a5b-b75a-7553-8dea-85e74e933ccd
subagents_drained: true
background_sessions_drained: true
---

# Western hub authored; Relay 07 may close deep Sunken and outer-west coverage

## Outcome

Relay 06 audited the exact seven-route `852 x 1846` phone footprint before
choosing bounds, then authored the smallest useful western hub/approach batch:

- `whispering-approach-912-v1` at `[512,1216,512,512]`;
- `greenhollow-hub-912-v1` at `[320,1664,512,512]`;
- `sunken-approach-912-v1` at `[224,2112,512,512]`;
- `greenhollow-millbrook-912-v1` at `[704,1728,512,512]`.

Composition references preceded generation. Four accepted `1254 x 1254` RGB
masters are genuinely new built-in image-generation outputs; rejected variants
with an invented coast, shifted Sunken assembly, or extra trail were excluded.
The new batch uses its own deterministic exact `57/32` shared lattice, 192-color
no-dither palette, directed overlap crossfades, exact `912 x 912` RGBA layers,
and 24-world feather. The accepted Relay 04/05 central-east directory remains
byte-identical across all 139 files.

No western affinity was added: exhaustive selection peaks stay within four.
The canonical seven-route harness passes with all endpoints reached, all 21
blockers engaged, zero browser or visible asset/detail errors, and no visible
rectangular seam, moving fuzzy boundary, stream pop, flash, input hitch, or boot
regression in the reviewed western motion. Fresh read-only review returned PASS
with no actionable findings.

## Verification state

PASS:

- deterministic four-region lattice double render;
- exact 30-chunk `2368 x 2912` reconstruction;
- all four shared-composited macro correlations above `0.45`;
- exhaustive 201-sample preload/draw selection at or below four details;
- seven-route path, blocker, bridge, collision, semantic, and save assertions;
- complete map-engine, retained-gate, snapshot, and runtime-override suite;
- exact `852 x 1846`, 25 fps traverse videos for all seven routes;
- explicit per-route resources, transfer/decoded bytes, residency, eviction,
  frame, input, miss, and boot telemetry;
- zero browser errors/misses, peaks of six chunks/four details, and dense 4 fps
  visual review for the three western routes;
- Python/JavaScript static checks and `git diff --check`.

Full metrics and verdict:
`runtime-v2/WESTERN-HUB-912-R06-VERIFICATION.md`.

Canonical evidence:
`runtime-v2/western-hub-912-r06/evidence/`.

Generation provenance and exact camera audit:
`runtime-v2/western-hub-912-v1/PROVENANCE.md` and
`runtime-v2/western-hub-912-v1/COVERAGE-AUDIT.md`.

## Locked decisions

- Work only in
  `/Users/christopherhachisu/Documents/claudecode/edu-rpg-map-engine-semantic-data`.
  `/Users/christopherhachisu/Documents` may be the workspace root.
- Preserve the shared dirty tree and every accepted Relay 01–06 artifact. Do
  not commit, push, deploy, publish, release, create a branch/worktree, alter
  TestFlight/App Store Connect, or expand permissions.
- Do not run Vite, `npm run build`, `npx vite`, or rebuild the legacy shipped
  source.
- Never sharpen or upscale legacy pixels. New authored coverage must begin with
  new high-resolution masters and use deterministic 912/512 reduction under the
  imagegen and game-design contracts.
- Preserve locked composition, all seven route geometries, bridges, blockers,
  semantic transitions/saves, geometry-only collision, cardinal-only G3,
  `125 ms` `0-A-0-B`, continuous analog input, speed `52`, camera width `208`,
  HUD, Port detail, retained landmarks, and the Crystal seal/mouth.
- Keep the hard six-chunk/four-detail runtime error. Use route/progress affinity
  only when exhaustive preload/draw sampling proves it is required; never hide
  overflow through ranking, truncation, blur, or runtime scaling.
- The western batch union is `[224,1216,1216,2624]`. It intentionally does not
  claim the complete Sunken phone union through bottom `2723.3141`; that exact
  residual and any remaining outer-west legacy exposure are Relay 07 inputs,
  not pre-approved bounds.

## Relay 07 single outcome

Audit the exact residual locked-phone authored-coverage gaps after the 13-region
manifest, then derive and author the smallest useful deep-Sunken and outer-west
912 batch that closes the highest-value remaining visible legacy footprint.
Do not pre-assume bounds, count, or affinity. Start from the canonical seven
route polylines, camera footprint, all accepted region unions, and exhaustive
preload/draw sampling. Capture composition references before generation and use
only genuinely new high-resolution masters through a separate deterministic
shared-lattice pipeline that cannot re-quantize accepted output.

Failable proof remains deterministic builders, accepted-artifact byte identity,
exact chunk reconstruction, the full path/map-engine/semantic suite, exact
`852 x 1846` affected-route and seven-route motion, explicit resource/resident/
stream telemetry, no fuzzy boundary, seam, pop, flash, asset miss, input hitch,
or boot regression, plus fresh read-only review. If complete coverage would
require more than the bounded batch or introduce a material owner decision,
record the exact residual and stop rather than broadening scope.

## Agent and background drain ledger

- `western_coverage_audit`: completed read-only camera-union and exhaustive
  ceiling audit; no files owned.
- `western_pipeline_audit`: completed read-only integration/ceiling review; no
  files owned.
- `western_lattice_builder`: completed the isolated deterministic builder in
  `scripts/build_act1_western_hub_lattices.py`; root inspected and verified it.
- `western_master_batch`: completed seven built-in image-generation calls and
  persisted four accepted masters plus provenance; root inspected all selected
  masters and rejected-candidate rationale.
- `western_r06_fresh_review`: completed read-only with PASS and no actionable
  findings after independent camera, render/hash, regression, telemetry,
  `ffprobe`, and dense-motion review.
- Canonical capture unified session `92736` completed with exit `0`.
- Owned static-server unified session `52882` stopped with Ctrl-C and exit `0`.
- Final collaboration re-list showed only completed children. No working,
  waiting, or idle child owns unfinished work, and no owned background session
  remains live.

## Paste-ready continuation prompt

`[relay:edu-rpg-act1-overhaul:07]` — **Act 1 Relay 07 — Deep-Sunken & Outer-West 912 Coverage**

Open `/Users/christopherhachisu/Documents` as the Codex workspace root if
useful, but work only in
`/Users/christopherhachisu/Documents/claudecode/edu-rpg-map-engine-semantic-data`.
Resume from the verified handoff at
`/Users/christopherhachisu/Documents/claudecode/edu-rpg-map-engine-semantic-data/docs/handoffs/2026-07-16-act1-western-hub-912-regions-relay.md`.

Use `$relay-fresh-sessions` and `$session-relay`. Also read the edu-rpg,
coding-skill, ponytail, game-design, imagegen, and code-check skills plus
repository `AGENTS.md`, `docs/AGENT-WORKFLOW.md`,
`docs/AUTONOMOUS-SESSION-RELAY.md`, `design/ART-DIRECTION.md`,
`design/GAME-FEEL.md`, runtime `ACT1-HIFI-RUNTIME-CONTRACT.md`,
`CENTRAL-EAST-912-R05-VERIFICATION.md`, and
`WESTERN-HUB-912-R06-VERIFICATION.md` before acting. Verify handoff metadata,
the dirty repository state, and absence of inherited live agents.

Single outcome: audit the exact residual seven-route locked-phone authored
coverage after the 13-region manifest, then derive and author the smallest
useful deep-Sunken and outer-west 912 batch that closes the highest-value
remaining visible legacy footprint. Do not pre-assume region bounds, master
count, or affinity. Capture composition references before image generation,
use genuinely new high-resolution masters, and integrate them through a
separate deterministic `912/512` shared lattice that preserves every Relay
01–06 byte. Preserve the hard six-chunk/four-detail ceiling; use route/progress
affinity only when exhaustive preload/draw sampling proves it is needed.

Failable proof: deterministic builders, accepted-artifact byte identity, exact
chunk reconstruction, full map-engine/path/semantic regression suites, exact
`852 x 1846` motion on affected routes and the full seven-route harness,
explicit resource/resident/streaming telemetry, no fuzzy fallback boundary,
seam, pop, flash, asset miss, input hitch, or boot regression, plus fresh
read-only review.

Locked decisions and non-goals: preserve the dirty shared working tree and
every accepted Relay 01–06 artifact; preserve locked composition, all seven
route geometries, bridges, blockers, semantic transitions/saves, geometry-only
collision, cardinal-only G3, `125 ms` `0-A-0-B`, continuous analog joystick,
speed `52`, camera width `208`, HUD, Port detail, retained landmarks, and the
Crystal seal/mouth. Never sharpen or upscale legacy pixels. Do not run Vite,
`npm run build`, `npx vite`, or rebuild the legacy shipped source. Do not
commit, push, deploy, publish, release, alter TestFlight/App Store Connect,
create a branch/worktree, expand dungeons, or modify retained landmarks. Work
autonomously within this non-destructive scope; ask only before destructive
actions or genuine owner decisions.

At the next verified boundary, drain every subagent and owned background
session, write the outgoing handoff, and use `$relay-fresh-sessions` to create
exactly one Relay 08 task only if all gates pass. Use
`$relay-fresh-sessions` at the next verified boundary.
