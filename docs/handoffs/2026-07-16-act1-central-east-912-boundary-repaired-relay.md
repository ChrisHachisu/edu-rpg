---
date: 2026-07-16
type: handoff
project: edu-rpg
milestone: act1-central-east-912-boundary-repaired
status: active
supersedes: docs/handoffs/2026-07-16-act1-central-east-912-regions-relay.md
relay_chain_id: edu-rpg-act1-overhaul
relay_sequence: "05"
relay_status: delegated
relay_predecessor_thread_id: 019f6966-c06b-7d72-9a99-72d405d12b90
relay_successor_thread_id: 019f6a0b-f34a-7563-9e66-6257a2fc1d72
subagents_drained: true
background_sessions_drained: true
---

# Central-east boundary repaired; Relay 06 may author the western hub

## Outcome

Relay 05 repaired the P1 north-fork authored-to-fuzzy boundary with five new
high-resolution continuation masters for the full Darkfang and Crystal Cave
approaches. The original three central-east regions plus these five now share
one deterministic `57/32` lattice and exact `912 x 912` runtime layers.

The formerly failing Darkfang `17.3..20.3 s` and Crystal `16.9..19.9 s`
intervals now remain sharp and continuous. Crystal Cave is not redesigned: a
visual-only circular exclusion preserves the legacy mouth, while collision,
seal, route, and semantic ownership remain in authored data.

Route/progress affinity keeps the streaming ceiling at six chunks and four
detail regions. The complete seven-route locked-phone harness passes with zero
browser errors, zero visible asset/detail misses, all endpoints reached, and
no visible seam, pop, flash, input hitch, or boot regression. Fresh read-only
review returned PASS with no actionable findings.

## Verification state

PASS:

- deterministic eight-region lattice build and exact rerender hashes;
- all eight shared-composited macro correlations above `0.45`;
- exact 30-chunk reconstruction and visual-exclusion alpha assertions;
- exhaustive per-route preload/draw sampling at or below four details;
- seven-route corridor, bridge, blocker, semantic, and save tests;
- full map-engine/runtime snapshot/override suite;
- exact `852 x 1846`, 25 fps traversal videos for all seven routes;
- Darkfang `15..23 s` and Crystal `14..20 s` 4 fps boundary contact sheets;
- zero runtime errors/misses and fresh read-only visual/code PASS.

Full metrics and verdict:
`runtime-v2/CENTRAL-EAST-912-R05-VERIFICATION.md`.

Canonical evidence:
`runtime-v2/central-east-912-r05/evidence/`.

## Locked decisions

- Work only in
  `/Users/christopherhachisu/Documents/claudecode/edu-rpg-map-engine-semantic-data`.
  The workspace root may remain `/Users/christopherhachisu/Documents`.
- Preserve the dirty worktree and every accepted Relay 01–05 artifact. No
  commit, push, deploy, publish, release, branch, worktree, TestFlight, or App
  Store Connect action.
- Do not run Vite, `npm run build`, `npx vite`, or rebuild the legacy shipped
  source.
- Never sharpen or upscale the old raster. New coverage must start from genuine
  high-resolution authored source and deterministic 912/512 reduction under
  the imagegen and game-design contracts.
- Preserve locked composition, all seven route geometries, bridges, blockers,
  semantic transitions/saves, geometry-only collision, cardinal-only G3,
  `125 ms` `0-A-0-B`, analog joystick, speed `52`, camera width `208`, HUD,
  Port detail, and the Crystal seal/mouth.
- Keep the hard six-chunk/four-detail ceiling. Do not hide overflow through
  ranking, truncation, blur, or runtime scaling.
- The remaining western legacy chunks are diagnostic fallback only; Relay 06
  must replace the selected western-hub slice with genuine authored coverage.

## Relay 06 outcome

Author the next bounded western-hub 912 region batch covering the highest-value
shared western route footprint (Greenhollow and the common approaches toward
Sunken Cellar, Whispering Woods, and Millbrook). Start with an exact locked
phone-viewport coverage audit; choose the smallest set of `512 x 512` regions
that materially removes visible western legacy coverage while respecting the
four-detail ceiling. Record composition references before generation, then use
new high-resolution masters and the existing deterministic lattice pipeline.

Do not pre-lock region bounds from this handoff. Derive them from the canonical
seven route polylines, camera footprint, current authored union, and overlap
budget. Preserve route/collision/semantic ownership, and use route/progress
affinity only when exhaustive sampling proves it is needed.

Failable proof remains exact locked-phone motion, deterministic builds, bounded
streaming/resource telemetry, no fuzzy boundary or stream pop, full regression
harness, and fresh read-only review.

## Agent and background drain ledger

- Coverage audit, master-generation batches, streaming consultant, composition
  consultant, retry worker, and final fresh reviewer all completed their
  bounded work; no child owns unfinished repository changes.
- Canonical capture unified session `25386` completed exit `0`.
- Owned static-server unified session `13349` stopped with Ctrl-C, exit `0`.
- Final fresh reviewer reported PASS with no actionable findings.
- Recheck the collaboration tree immediately before successor creation and
  retain `subagents_drained: true` only while no agent is active.

## Paste-ready continuation prompt

`[relay:edu-rpg-act1-overhaul:06]` — **Act 1 Relay 06 — Western-Hub 912 Region Batch**

Open `/Users/christopherhachisu/Documents` as the Codex workspace root, but
work only in
`/Users/christopherhachisu/Documents/claudecode/edu-rpg-map-engine-semantic-data`.
Resume from
`/Users/christopherhachisu/Documents/claudecode/edu-rpg-map-engine-semantic-data/docs/handoffs/2026-07-16-act1-central-east-912-boundary-repaired-relay.md`.
Read the relay/session, edu-rpg, coding, ponytail, game-design, imagegen, and
code-check skills plus repository relay, workflow, art-direction, game-feel,
runtime-contract, and Relay 05 verification documents before acting.

Single outcome: derive and author the smallest useful western-hub 912 region
batch from exact seven-route phone-viewport coverage, while keeping the hard
six-chunk/four-detail ceiling. Capture composition references before imagegen;
use genuinely new high-resolution masters and deterministic 912/512 reduction.
Do not upscale/sharpen legacy pixels, change route/collision/semantic ownership,
alter locked gameplay/UI/hero behavior, or pre-assume region bounds.

Run all mechanical gates sequentially, capture exact `852 x 1846` motion for
affected routes and the full harness, inspect transitions in motion, obtain a
fresh read-only PASS, drain all agents/background sessions, then use the relay
skill to create exactly one Relay 07 task only if the slice passes. Use
`$relay-fresh-sessions` at the next verified boundary.
