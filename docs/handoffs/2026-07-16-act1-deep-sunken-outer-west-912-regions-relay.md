---
date: 2026-07-16
type: handoff
project: edu-rpg
milestone: act1-deep-sunken-outer-west-912-regions
status: active
supersedes: docs/handoffs/2026-07-16-act1-western-hub-912-regions-relay.md
relay_chain_id: edu-rpg-act1-overhaul
relay_sequence: "07"
relay_status: delegated
relay_predecessor_thread_id: 019f6a5b-b75a-7553-8dea-85e74e933ccd
relay_successor_thread_id: 019f6a8e-f6c5-7c01-891e-a20a1b5fb949
subagents_drained: true
background_sessions_drained: true
---

# Deep Sunken and outer-west authored; Relay 08 may close Coastal Reef coverage

## Outcome

Relay 07 audited the exact residual seven-route `852 x 1846` phone footprint
after the 13-region manifest and authored the smallest useful two-region batch:

- `sunken-deep-912-v1` at `[224,2240,512,512]`;
- `millbrook-outer-west-912-v1` at `[1152,1952,512,512]`.

The first 32-world Sunken origin that clears exact camera bottom `2723.3141` is
`2240`; `2208` ends at `2720` and fails. The Millbrook region closes the exact
lower-right residual shared by the Greenhollow-Millbrook endpoint and
Millbrook-Port start. All three affected endpoints now have complete rectangular
authored coverage. No route/progress affinity was added.

Composition references were captured and inspected before generation. Five
built-in image-generation calls produced two accepted, genuinely new `1254 x
1254` RGB masters; three candidates with shifted/invented retained geometry were
rejected. A separate deterministic exact `57/32` shared coordinate lattice,
batch-global 192-color no-dither palette, exact `912 x 912` RGBA layers, and
24-world feather preserve every accepted Relay 01-06 byte.

## Verification

PASS:

- double-render byte identity for the complete Relay 07 batch;
- macro composition correlations `0.851` and `0.608` above the `0.45` gate;
- all 210 accepted Port/central-east/western artifacts byte-identical;
- accepted central-east/western builders and path module byte-identical;
- all 90 prior chunk-layer files byte-identical after manifest rebuild;
- exact 30-chunk `2368 x 2912` RGB reconstruction;
- exhaustive selection peaks `4/4`, `4/3`, `4/3`, `4/4`, `2/2`, `4/4`, `4/4`;
- full map-engine, path, collision, blocker, semantic, retained-gate, save,
  snapshot, and runtime-override suites;
- exact `852 x 1846`, 25 fps videos for all seven routes plus cardinal motion;
- all seven endpoints and all 21 blocker probes; zero browser/chunk/detail misses;
- six-chunk/four-detail hard ceilings, explicit transfer/decoded bytes,
  resident pixels, evictions, frames, input latency, and boot telemetry;
- three complete-route dense `4 fps` affected-route contact sheets with no
  fuzzy boundary, rectangular seam, pop, post-readiness flash, asset loss,
  input hitch, or retained-composition break;
- Python/JavaScript static checks, `git diff --check`, code check, and fresh
  read-only review with no actionable findings.

Full metrics and verdict:
`runtime-v2/DEEP-SUNKEN-OUTER-WEST-912-R07-VERIFICATION.md`.

Canonical evidence:
`runtime-v2/deep-sunken-outer-west-912-r07/evidence/`.

Coverage/provenance:
`runtime-v2/deep-sunken-outer-west-912-v1/COVERAGE-AUDIT.md` and
`runtime-v2/deep-sunken-outer-west-912-v1/PROVENANCE.md`.

## Current state

- Worktree remains deliberately dirty on branch `codex/map-engine-semantic-data`.
- No commit, push, deploy, publish, release, branch/worktree creation, Vite run,
  legacy rebuild, or TestFlight/App Store Connect mutation occurred.
- Manifest is revision `7`; path constraints remain revision `2`.
- Full Act 1 authored replacement remains explicitly false. The bounded Coastal
  Reef continuation is the clearest high-value remaining visible legacy footprint.

## Locked decisions

- Work only in
  `/Users/christopherhachisu/Documents/claudecode/edu-rpg-map-engine-semantic-data`.
- Preserve the shared dirty tree and every accepted Relay 01-07 artifact.
- Do not run Vite, `npm run build`, `npx vite`, or rebuild legacy shipped source.
- Never sharpen or upscale legacy pixels. New coverage starts with genuinely
  new high-resolution masters and deterministic `912/512` reduction.
- Preserve locked composition, seven route geometries, bridges, blockers,
  semantic transitions/saves, geometry-only collision, cardinal-only G3,
  `125 ms` `0-A-0-B`, continuous analog input, speed `52`, camera width `208`,
  HUD, Port detail, retained landmarks, and Crystal seal/mouth.
- Keep the hard six-chunk/four-detail runtime error. Use route/progress affinity
  only if exhaustive preload/draw sampling proves it is necessary.
- Do not commit, push, deploy, publish, release, alter TestFlight/App Store
  Connect, create a branch/worktree, expand dungeons, or modify retained landmarks.

## Remaining work

Audit the exact residual locked-phone authored coverage after the 15-region
manifest, then derive the smallest useful Coastal Reef 912 batch that closes
the highest-value remaining visible legacy footprint. Do not pre-assume bounds,
master count, or affinity. Stop rather than broaden scope if complete closure
requires an owner decision or exceeds a bounded coherent image batch.

## Agent drain ledger

- `r07_coverage_audit`: interrupted without a usable return; read-only, no files.
- `r07_pipeline_audit`: interrupted without a usable return; read-only, no files.
- `r07_lattice_builder`: completed the isolated builder in
  `scripts/build_act1_deep_sunken_outer_west_lattices.py`; root inspected and
  verified the file and generated outputs.
- `r07_master_batch`: completed five built-in generation calls, persisted two
  accepted masters byte-identically, and recorded three rejection reasons.
- `r07_fresh_review`: completed read-only PASS with no actionable findings after
  independent geometry, hash, reconstruction, regression, video, telemetry,
  visual, and code review.
- Final collaboration re-list showed only completed children; no working,
  waiting, or idle child owns unfinished work.

## Background session drain ledger

- Canonical capture unified session `57646` completed with exit `0`.
- Owned static-server unified session `40102` stopped with Ctrl-C and exit `0`.
- All other command cells completed in the foreground. No owned watcher,
  recorder, compiler, server, or shell remains live.

## Resume here

Read this handoff and the named runtime contract/Relay 07 verification, inspect
the dirty tree and revision-7 manifest, confirm no inherited live agents, then
start with an exact seven-route residual audit. Capture composition references
before image generation and preserve all accepted bytes.

## Paste-ready continuation prompt

`[relay:edu-rpg-act1-overhaul:08]` — **Act 1 Relay 08 — Coastal Reef 912 Coverage**

Open `/Users/christopherhachisu/Documents` as the Codex workspace root if useful,
but work only in
`/Users/christopherhachisu/Documents/claudecode/edu-rpg-map-engine-semantic-data`.
Resume from the verified handoff at
`/Users/christopherhachisu/Documents/claudecode/edu-rpg-map-engine-semantic-data/docs/handoffs/2026-07-16-act1-deep-sunken-outer-west-912-regions-relay.md`.

Use `$relay-fresh-sessions` and `$session-relay`. Also read the edu-rpg,
coding-skill, ponytail, game-design, imagegen, and code-check skills plus parent
and project `AGENTS.md`, `docs/AGENT-WORKFLOW.md`,
`docs/AUTONOMOUS-SESSION-RELAY.md`, `design/ART-DIRECTION.md`,
`design/GAME-FEEL.md`, runtime `ACT1-HIFI-RUNTIME-CONTRACT.md`,
`WESTERN-HUB-912-R06-VERIFICATION.md`, and
`DEEP-SUNKEN-OUTER-WEST-912-R07-VERIFICATION.md` before acting. Verify handoff
metadata, the dirty repository state, manifest revision 7, accepted artifact
hashes, and absence of inherited live agents.

Single outcome: audit the exact residual locked-phone authored coverage after
the 15-region manifest, then derive and author the smallest useful Coastal Reef
912 batch that closes the highest-value remaining visible legacy footprint. Do
not pre-assume region bounds, master count, or affinity. Capture composition
references before image generation, use genuinely new high-resolution masters,
and integrate through a separate deterministic `912/512` shared lattice that
preserves every Relay 01-07 byte. Preserve the hard six-chunk/four-detail
ceiling; use route/progress affinity only when exhaustive preload/draw sampling
proves it is needed.

Failable proof: deterministic builders, accepted-artifact byte identity, exact
chunk reconstruction, full map-engine/path/semantic regression suites, exact
`852 x 1846` affected-route and seven-route motion, explicit resource/resident/
stream telemetry, no fuzzy fallback boundary, seam, pop, flash, asset miss,
input hitch, or boot regression, plus fresh read-only review.

Locked decisions and non-goals: preserve the dirty shared working tree and every
accepted Relay 01-07 artifact; preserve locked composition, all seven route
geometries, bridges, blockers, semantic transitions/saves, geometry-only
collision, cardinal-only G3, `125 ms` `0-A-0-B`, continuous analog joystick,
speed `52`, camera width `208`, HUD, Port detail, retained landmarks, and the
Crystal seal/mouth. Never sharpen or upscale legacy pixels. Do not run Vite,
`npm run build`, `npx vite`, or rebuild legacy shipped source. Do not commit,
push, deploy, publish, release, alter TestFlight/App Store Connect, create a
branch/worktree, expand dungeons, or modify retained landmarks. Work
autonomously within this non-destructive scope; ask only before destructive
actions or genuine owner decisions.

At the next verified boundary, drain every subagent and owned background
session, write the outgoing handoff, and use `$relay-fresh-sessions` to create
exactly one Relay 09 task only if all gates pass. Use `$relay-fresh-sessions` at
the next verified boundary.
