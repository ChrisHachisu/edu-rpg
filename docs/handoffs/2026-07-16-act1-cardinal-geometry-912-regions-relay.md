---
date: 2026-07-16
type: handoff
project: edu-rpg
milestone: act1-cardinal-geometry-912-regions
status: active
supersedes: docs/handoffs/2026-07-16-act1-912-hero-g3-traversal-relay.md
relay_chain_id: edu-rpg-act1-overhaul
relay_sequence: "04"
relay_status: delegated
relay_predecessor_thread_id: 019f6891-9ce7-7e50-8a06-6ccf2154c7ba
relay_successor_thread_id: 019f68fd-8b88-7193-aa3a-8d9c7a96b8eb
subagents_drained: true
background_sessions_drained: true
---

# Cardinal traversal locked; authored 912 region batches next

## Outcome

Relay 03 completed the compatible movement/geometry boundary:

- all seven production Act 1 routes now have geometry-owned art-space
  centerlines, variable widths, semantic spine/threshold separation, three
  route/landmark probes, exact shared anchors, and a retained Crystal seal;
- continuous normalized 360-degree steering remains at speed `52`, but G3
  runtime animation is cardinal-only with stable dominant-axis hysteresis;
- G3 stays native `64 px`, drawn at `36` world pixels from the unchanged
  `192x512` sheet, with restrained crystal shield/armor hints;
- cadence is owner-locked at `125 ms` per `0-A-0-B` pose;
- the four-arrow pad is now one reactive analog base/knob with keyboard parity,
  continuous displacement, release/cancel recentering, and `12 ms` captured
  first response;
- camera `208`, 912 Port detail, HUD, streamer budgets, topology, transitions,
  saves, bridges, exclusion zones, and geometry-only collision remain locked.

## Verification

PASS:

- `node scripts/test_act1_path_corridors.mjs`
- `python3 scripts/test_act1_hifi_chunks.py`
- `pnpm run test:map-engine`
- JS syntax checks and Python bytecode compilation
- exact `852x1846` motion for seven routes, 21 blocker probes, four cardinal
  facings at 125ms, analog active/recenter, delayed boot, and streaming metrics
- fresh read-only visual review after the corrected Port/Darkfang/Crystal road
  trace

Evidence:

- `runtime-v2/FULL-WORLD-TRAVERSAL-R3-VERIFICATION.md`
- `runtime-v2/full-world-traversal-r3/evidence/`

Measured: peak `6` chunks / `1` detail region, peak decoded resident equivalent
`28,855,296` bytes, `21..42` resource requests per route, zero visible misses,
zero browser errors, route/detail eviction exercised. Two isolated capture-frame
samples exceeded 34ms across 7,191 frames; corrected Darkfang/Crystal runs had
no slow frames and review found no visible hitch.

## Hard failure / remaining work

Full Act 1 912 presentation is **NOT DONE**. Only
`port-sapphire-pixel-source-912-v1` is genuinely authored at the locked lattice.
All non-Port traversal falls back to the old fuzzy base, and Millbrook-to-Port
shows a bounded fuzzy/detail transition. Zero asset misses do not make that
acceptable.

The next relay begins coherent authored-region replacement. First batch should
close the most visible Port boundary: Millbrook-to-Port plus the shared north
fork used by Port-to-Darkfang/Crystal, stopping before Crystal Cave. Preserve
the locked composition and the verified geometry; never sharpen or upscale the
old raster. Generate genuinely new high-resolution region masters with the
imagegen/game-design workflows, then deterministically reduce to `912/512` and
stream as bounded detail regions with feather-safe seams.

## Locked decisions

- User granted full access. Do not request permission for in-scope,
  non-destructive work; ask only before destructive actions or when owner input
  is genuinely required.
- Preserve the dirty worktree. No commit, push, deploy, release, TestFlight,
  branch, or worktree creation.
- No Vite, `npm run build`, `npx vite`, or legacy-source rebuild.
- No dungeon expansion and no Crystal Cave modification.
- World movement is continuous analog; animation rows are only down/left/up/right.
- Walk cadence is 125ms; do not reopen taste iteration absent a demonstrated
  failure.
- Collision is geometry-owned and never samples pixels.
- Old fuzzy raster is a hard failure, never a production fallback.
- No giant always-resident world bitmap. Keep explicit loaded-region/chunk
  budgets, eviction, startup requests, bytes/resident pixels, frame pacing,
  input latency, exact-phone motion, seam/feather, and boot gates.

## Resume here

Read this handoff, repo `AGENTS.md`, `docs/AGENT-WORKFLOW.md`,
`docs/AUTONOMOUS-SESSION-RELAY.md`, `design/ART-DIRECTION.md`,
`design/GAME-FEEL.md`, runtime contract, R3 verification, manifest/builder, and
current evidence. Audit dirty state and drains. Then author and integrate the
first central-east 912 region batch adjacent to Port without changing route
geometry or Crystal Cave. Stop only for a genuine new composition choice; at a
verified batch boundary, drain and relay the next coherent region.

## Kickoff prompt

`[relay:edu-rpg-act1-overhaul:04]` — **Act 1 Relay 04 — Central-East 912 Region Batch**

Work only in `/Users/christopherhachisu/Documents/claudecode/edu-rpg-map-engine-semantic-data`
and resume from `docs/handoffs/2026-07-16-act1-cardinal-geometry-912-regions-relay.md`.
Read `/Users/christopherhachisu/.codex/skills/relay-fresh-sessions/SKILL.md`, repo
`AGENTS.md`, `docs/AGENT-WORKFLOW.md`, `docs/AUTONOMOUS-SESSION-RELAY.md`,
`design/ART-DIRECTION.md`, `design/GAME-FEEL.md`, runtime
`ACT1-HIFI-RUNTIME-CONTRACT.md`, and `FULL-WORLD-TRAVERSAL-R3-VERIFICATION.md`
before acting. User granted full access: do not ask permission for in-scope
non-destructive work; ask only before destructive actions or for genuinely
required owner input.

Single outcome: author and stream the first coherent non-Port 912-lattice batch
covering Millbrook-to-Port and the shared north fork toward Darkfang/Crystal,
stopping before Crystal Cave. Use imagegen + game-design for genuinely new
high-resolution masters and deterministic `912/512` palette/lattice reduction.
Locked composition and the verified seven-route geometry are invariants; do not
sharpen/upscale the old raster or sample pixels for collision.

Failable proof: exact `852x1846` motion across every new region boundary; no
fuzzy fallback inside the batch, seam/feather pop, black flash, occlusion pop,
asset miss, input hitch, or boot regression; explicit startup requests,
transferred/decoded bytes or resident-pixel equivalent, peak chunks/detail
regions, eviction, and frame pacing. Preserve cardinal-only G3, 125ms 0-A-0-B,
analog joystick, speed52, camera208, HUD, semantic transitions/saves, bridges,
blockers, Port detail, and dirty tree. Obtain fresh read-only review.

Do not use Vite/legacy rebuild, commit/push/deploy/release, create a branch or
worktree, expand dungeons, or modify Crystal Cave. At the next verified boundary
use `$relay-fresh-sessions` and the project relay rule for the next authored
region batch.
